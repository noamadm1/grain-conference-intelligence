# PRD — Conference Intelligence Tool for Grain

> מסמך אפיון מוצר. מסמך חי — מתעדכן תוך כדי בנייה.
> **גרסה 1.1** — מעודכן למה שנבנה בפועל (ספטמבר 2026). שינויים מהתוכנית המקורית מפורטים בסעיף 15.
> סימון: **⏳ לא נבנה** = חלק מהתוכנית שעוד לא מומש.

---

## 1. רקע ומטרה

### על Grain
Grain עוזרת לנהל סיכוני מטבע (FX). **ה-ICP: פלטפורמות ומרקטפלייסים, חברות שמזיזות כסף בשם אחרים** (מהראיון עם Grain):

| קהל | למה ICP |
|---|---|
| מרקטפלייסים, פלטפורמות הזמנה ו-OTAs, פלטפורמות gig | גובים במטבע אחד ומשלמים לספקים במטבע אחר, בשם אחרים |
| PSPs וחברות תשלומים חוצי גבולות | כל הפעילות היא העברת כסף של לקוחות בין מטבעות |
| פינטק משובץ, SaaS ורטיקלי עם תשלומים | ICP חלקי: פלטפורמה רק אם מזיזים כסף של הלקוחות |

**הקריטריון הוא "מזיז כסף של אחרים", לא "יש לו חשיפת מט"ח" ולא תעשייה.**
Booking.com, ולא המלון. עסק בודד עם חשיפה (יבואן, סיטונאי תיירות, מחלקת טרז'רי) מגדר לעצמו ואינו ה-ICP.

### הבעיה
כנסים הם ערוץ מרכזי ליצירת צנרת. היום המידע מפוזר בין אקסל, Slack ומחברות אישיות. התוצאות:
- חודשים שלמים עוברים בלי כיסוי
- אזורים שלמים לא מטורגטים
- שתי נסיעות נפרדות לאותה עיר באותו חודש
- לידים מכנסים נעלמים בשרשורי Slack
- אף אחד לא זוכר שפגשנו את אותו אדם שלוש שנים ברצף

### המטרה
כלי אחד שמלווה את איש המכירות לאורך כל מחזור החיים של כנס: **בחירה → תכנון → ביצוע בשטח → מעקב**.

### עקרונות מנחים
1. **המערכת עושה את העבודה ומגישה מסקנה.** לא מציגה נתונים גולמיים.
2. **לאיש המכירות יש 30 שניות.** לא חוסר יכולת — סדר עדיפויות נכון.
3. **AI מחלץ ומנסח. לא מסיק ולא מכריע.**
4. **מסקנות למעלה, נתונים מאחוריהן.**
5. **מתכננים לתרחיש הגרוע.** אין רשת, ה-QR לא עובד, הצילום נכשל.

---

## 2. ארכיטקטורה

### Stack
| שכבה | טכנולוגיה | נימוק |
|---|---|---|
| Frontend | אתר סטטי (React 19 + Vite, React Router עם HashRouter) | פריסה בגרירה של תיקיית `dist` ל-Netlify/Vercel, בלי הגדרות הפניה |
| DB | Supabase (Postgres) | REST API אוטומטי, ללא שרת |
| קבצים | Supabase Storage, bucket פרטי `recordings` | הקלטות קוליות |
| Serverless | Supabase Edge Functions | מתווך ל-HubSpot (בעיית CORS). ⏳ Cron לא נבנה |
| תמלול | OpenAI Whisper (`whisper-1`, עברית) | |
| חילוץ ופרשנות | OpenAI `gpt-4o-mini` (JSON Schema במצב strict) | במקום Claude, ראו סעיף 15 |
| CRM | HubSpot Imports API | דרך Edge Function. הורדת CSV תמיד זמינה |

### אילוצים מהבריף
- **פריסה ללא build pipeline מורכב.** לא-מפתח צריך להיות מסוגל לארח ולעדכן. `npm run build`, ואז גוררים את `dist`.
- **מפתחות API מוגדרים על ידי המשתמש, לא בקוד.** מסך הגדרות בממשק: מפתח OpenAI (חובה ל-AI) ו-HubSpot token (לא חובה). נשמרים ב-localStorage של הדפדפן בלבד. לא ב-`.env` ולא בבאנדל.
- ב-`.env` יש רק את כתובת Supabase ואת המפתח הציבורי (publishable). מפתח ה-service role משמש רק את סקריפטי הטעינה ואינו נכנס לבאנדל.
- לא-מפתח מעדכן דאטה דרך טבלאות Supabase (כמו אקסל), לא דרך קוד. כך גם הגדרות נוסחת הציון (`app_settings`).

### סקריפטים
| פקודה | מה עושה |
|---|---|
| `npm run seed` | טוען את `seed/series.json` ו-`seed/editions.json` (upsert לפי id, בטוח להרצה חוזרת) |
| `npm run score` | מחשב ציון ICP למהדורות העתידיות, כותב ציון, הסבר ופירוק, ורושם שינויים ב-`score_history` |
| `npm run seed:demo` | דאטת דמו לאנשים ולמפגשים (סעיף 8). אידמפוטנטי ולא נוגע בדאטה אמיתית |
| `:dry` | גרסת "הרצה יבשה" של `seed` ו-`score`, בלי כתיבה |

### סיכונים ידועים
- **פרויקט Supabase חינמי מושהה לאחר חוסר פעילות.** פתרון מתוכנן: Cron keep-alive. ⏳ לא נבנה.
- **אין API לרשימות משתתפים בכנסים.** המערכת לא מניחה שיש.
- **אין התחברות.** המפתח הציבורי מאפשר קריאה וכתיבה לכל הטבלאות. מקובל לכלי פנימי, אבל כל מי שיש לו את הקישור יכול לשנות דאטה.

---

## 3. מודל נתונים

### `conference_series` — סטטי
```
id                  slug ("money2020-usa")
name
brand               לקיבוץ מהדורות ("money2020")
vertical
region              europe | north-america | apac | middle-east | africa | global-rotating
audience_mix        { platforms: 8, payments_psp: 35, embedded_fintech: 12,
                      saas_vertical: 3, treasury: 10, travel_wholesale: 0,
                      travel_general: 0, banks: 25, other: 7 }
                    אחוזים. סך הכל 100 בדיוק, כולל other
seniority_pct       אחוז מקבלי החלטות
has_expo_floor
has_meeting_system
meeting_system_name
has_attendee_list
has_evening_events
website
notes
data_confidence
manually_set[]      שדות שנערכו ידנית — Cron לא דורס
```

### `conference_editions` — דינמי
```
id                  slug + שנה ("money2020-usa-2026")
series_id
start_date, end_date, duration_days
city, country, venue
ticket_cost_usd     (במקור: ticket_cost)
attendees           { min, max, value, spread, sources[] }
exhibitors, companies, meetings_facilitated
status              upcoming | historical
icp_score           מספר שלם
icp_explanation     משפט ההסבר לציון
icp_breakdown       { audience, seniority, access, geo, penalty, fit, icp_volume }
confidence          { dates, venue, attendees, ticket_cost }: verified | estimated
last_verified
notes
manually_set[]
```
**מהדורות היסטוריות (3 שנים אחורה)** נטענות כעוגן ללידים. לא מדורגות, לא מרועננות, לא בתצוגת התכנון.

### `score_history`
```
edition_id, old_score, new_score, reason, created_at
```

### `app_settings` — חדש
```
key, value (jsonb)
```
המפתח `icp_formula` מחזיק את כל המשקלים והספים של נוסחת הציון (סעיף 5).

### `edition_assignments` — חדש
```
edition_id, rep_name, created_at     ייחודי לכל זוג
```
שיבוץ אנשי מכירות לכנסים. הבסיס לתצוגת התכנון (כיסוי, אשכולות, התנגשויות).

### `people`
```
id
phone           E.164 — המזהה הראשי
email, linkedin_name, linkedin_url
first_name, last_name
current_company, current_title
status          active | archived
created_at
```

### `encounters` — מפגש בכנס
```
id
person_id, edition_id
rep_name                שם איש המכירות (טקסט, אין טבלת נציגים)
company, title          כפי שהיו באותו מפגש
transcript              התמלול המלא
identity_line           משפט זיהוי אחד
extracted               { pain, timing, currencies[], authority, next_step }
audio_path              נתיב ההקלטה ב-Storage
external_id             מ-QR, אם היה
created_at
```
**מפגש עם `audio_path` ובלי `extracted` = ממתין לעיבוד AI.** המצב נשמר ב-DB, לא בזיכרון.

### `merge_decisions`
```
person_a, person_b, decision (same | different), decided_by, created_at
```
כדי שהמערכת לא תשאל פעמיים. ⏳ הטבלה קיימת, ממשק שאלת המיזוג לא נבנה.

---

## 4. רכיב 1 — מאגר הכנסים

### מקור הדאטה
- **Seed ידני מאומת:** 22 סדרות ו-36 מהדורות. 23 מהדורות עתידיות (אוקטובר 2026 עד אוקטובר 2027) ו-13 היסטוריות (2024–2026)
- **לא** 10times (אין API רשמי, רק scrapers בתשלום מאחורי Cloudflare)
- **לא** Eventbrite (Event Search API נסגר בדצמבר 2019, ללא תחליף)

### תערובת מכוונת
| קטגוריה | כמות |
|---|---|
| תשלומים / PSPs | 7-8 |
| תיירות | 4-5 |
| טרז'רי / כספים | 3-4 |
| אזוריים וקטנים | 3-4 |
| **רחבים ולא מתאימים** | 2-3 |

האחרונים חיוניים: **מאגר שבו כל הכנסים טובים לא מוכיח שהדירוג עובד.** בפועל Web Summit, Collision ו-SaaS North מקבלים 31–40.

### אמינות נתונים
- כל נתון מספרי נשמר כטווח ממספר מקורות. הערך המוצג = האמצע.
- **הפיזור הוא מדד האמינות:** מתחת ל-15% אמין · מעל 30% עם אזהרה (⚠️ ליד מספר המשתתפים)
- מעבר עכבר על מספר המשתתפים חושף מקורות

### מנגנוני Cron
| משימה | תדירות | פעולה |
|---|---|---|
| Keep-alive | כל 3 ימים | `SELECT 1` |
| רענון מאגר | 1 לחודש | מאמת כנסים שלא אומתו מעל חודשיים |
| Conference Brief | יומי | בודק אם יש כנס בעוד 7 ימים |

⏳ **אף משימת Cron לא נבנתה.** בינתיים חישוב הציון רץ ידנית (`npm run score`), וכל שינוי נרשם ב-`score_history` עם שורת הסבר.

### עריכה ידנית
פילוח קהל ניתן לעריכה — **רק למנהל**. שדה שנערך מסומן ב-`manually_set` וה-Cron לא נוגע בו. סקריפט הציון מדלג על מהדורה ש-`icp_score` שלה מסומן ב-`manually_set`.
**ידע אנושי מנצח הערכה אוטומטית.** ⏳ אין ממשק עריכה. עורכים ישירות בטבלה ב-Supabase.

### ממשק (נבנה)
- **ארבעה פילטרים בשורה אחת, כתפריטים נפתחים:** המלצה (חובה / שווה את זה / רק אם כבר באזור / לוותר) · אזור · תאריך (רבעון או חודש) · עלות (טווח מ-עד). בתוך קבוצה "או", בין קבוצות "וגם". פילטר פעיל מוצג על הכפתור (*"אזור: אירופה, אסיה-פסיפיק"*), וכל אפשרות בפאנל מציגה כמה כנסים יתקבלו. "נקה הכל". במובייל הפאנל ברוחב מלא.
- **כל כרטיס נקי לסריקה:** ציון, שם הכנס (קישור לאתר, בלשונית חדשה), תג המלצה, תאריכים, עיר, עלות. אזור ומספר משתתפים כמטא-דאטה משנית.
- **הנימוק מאחורי הציון בכפתור (i) ליד הציון:** מה הציון מודד, ולכל רכיב הנקודות שלו ושורת נימוק מהדאטה (סעיף 5).
- ציון מעוגל, ללא עשרוניות. ממוין מהגבוה לנמוך
- שיבוץ אנשי מכירות ישירות מהכרטיס ("+ שבץ")

---

## 5. רכיב 2 — נוסחת ה-ICP

### העיקרון
הציון מודד **התאמת קהל בלבד**. העלות נפרדת.

**למה:** Money20/20 מצוין ויקר. כנס אזורי בינוני וזול. אם נדחוס מחיר לציון — שניהם יקבלו 65, ושתי המסקנות הפוכות.

### המבנה
```
ציון = איכות קהל (50)
       + [בכירות (10) + נגישות (30) + גיאוגרפיה (10)] × (0.5 + 0.5 × fit)
       − עונש מסה קריטית
```
**כל המשקלים והספים נשמרים ב-`app_settings` (מפתח `icp_formula`), לא בקוד.** שינוי בטבלה ואז `npm run score`, והציונים מתעדכנים. הקוד מכיל ברירות מחדל בלבד, שנכתבות לטבלה בהרצה הראשונה.

### א. איכות הקהל — 50
```
fit = Σ (אחוז הסגמנט × המשקל)
ציון = fit × 50
```

#### הגדרת ה-ICP: פלטפורמות, לא עסקים בודדים
**Grain מוכרת לפלטפורמות ולמרקטפלייסים: חברות שמזיזות כסף בשם אחרים.** Booking, לא המלון. (מהראיון עם Grain.)
עסק בודד עם חשיפת מט"ח, גדולה ככל שתהיה, **אינו ה-ICP**. השאלה היא לא "כמה מט"ח יש לו" אלא "האם הוא מזיז כסף של אחרים".

| סגמנט (מפתח ב-`audience_mix`) | משקל | נימוק |
|---|---|---|
| **פלטפורמות** — מרקטפלייסים, פלטפורמות הזמנה ו-OTAs, פלטפורמות gig (`platforms`) | **1.0** | הליבה: מזיזים כסף בשם אחרים |
| **PSPs ותשלומים חוצי גבולות** (`payments_psp`) | **1.0** | גם הם פלטפורמות |
| פינטק משובץ — embedded finance, BaaS, פינטק ורטיקלי (`embedded_fintech`) | 0.7 | בדרך כלל מזיזים כסף של הלקוחות שלהם, אבל לא תמיד חוצה מטבעות |
| SaaS ורטיקלי — תוכנה ענפית שעשויה להטמיע תשלומים (`saas_vertical`) | 0.5 | פלטפורמה בפוטנציה, רק אם מטמיעה תשלומים |
| טרז'רי (`treasury`) | 0.3 | **היה 0.85.** מנהלי כספים של חברות בודדות, שמגדרות לעצמן |
| סיטונאי תיירות — טור אופרייטורים, סיטונאים (`travel_wholesale`) | 0.3 | **היה 0.9.** עסקים בודדים. ה-OTAs עברו ל-`platforms` |
| **בנקים** (`banks`) | **0.1** | מתחרה, לא לקוח. עסק שרוצה לגדר מט"ח הולך היום לבנק שלו, ו-Grain מחליפה את זה. לא 0, כי כנסים עם הרבה בנקים עדיין נותנים מודיעין תחרותי וערך של שותפויות |
| תיירות כללית — מלונות, סוכנים, יעדים (`travel_general`) | 0.1 | המלון, לא Booking |
| אחר (`other`) | 0 | משקיעים, רגולטורים, ספקים, קמעונאים, פינטק שאינו משובץ (ניאו-בנקים, קריפטו, הלוואות) |

`fintech_general` (0.25) **הוסר**. הוא פוצל לפי מה שכל כנס עוסק בו: פינטק משובץ, SaaS ורטיקלי, ומה שלא מזיז כסף של אחרים עבר ל-`other`.

#### איך נקבעה החלוקה בכל כנס
- כל `audience_mix` מסתכם ל-100 בדיוק, כולל `other` מפורש.
- **נשמרו כמו שהיו:** בנקים, טרז'רי ותיירות כללית. אלה אותם אנשים, רק המשקל שלהם השתנה.
- **פלטפורמות** נלקחו מתוך ה-PSP והתיירות הקודמים (OTAs, מרקטפלייסים) ומה-`fintech_general`, לפי אופי הכנס. למשל: MPE ו-Seamless עם נוכחות מרקטפלייסים, Money20/20 Asia עם סופר-אפליקציות, Phocuswright עם OTAs ו-metasearch.
- ⚠️ **החלוקה היא הערכה שלי לפי תוכן הכנס ורשימות הדוברים, לא נתון מדוד.** אין מקור ציבורי שמפלח משתתפים לפלטפורמות מול עסקים בודדים. ראו סעיף 12.

### ב. בכירות — 10 (היה 20)
```
(אחוז מקבלי החלטות ÷ 67%) × 10    [מוגבל ל-10]
```
67% הוא הגבול הריאלי. Money20/20 עם 33% מקבל 5.
**למה ירד ל-10:** איש מכירות טוב נכנס דרך מי שהוא פוגש ומטפס משם למקבל ההחלטות. בכירות היא יתרון, לא תנאי. במשקל 20 היא החזיקה כנסים עם קהל חלש (EuroFinance: קהל 13 מתוך 45, בכירות 16).

### ג. נגישות לפגישות — 30 (היה 25)
| מרכיב | נק' |
|---|---|
| רשימת משתתפים זמינה מראש | 10 |
| מערכת קביעת פגישות | 8 |
| רצפת תערוכה | 6 |
| 3+ ימים | 4 |
| אירועי ערב | 2 |

החלוקה הפנימית הוגדלה כך שתסתכם ל-30, באותו סדר חשיבות כמו קודם.

**הניקוד הגבוה למה שמאפשר הכנה מראש** — שם ההבדל בין 6 שיחות ל-40.

### ד. גיאוגרפיה — 10
שוק בפוקוס: כן (10) או לא (0). מגיע מהגדרות המערכת, לא מה-AI.
- **שווקים בפוקוס:** אירופה וצפון אמריקה (ברירת מחדל, ניתן לשינוי). ⚠️ הנחה שלי, צריך לאשר מול Grain.
- **אין רכיב עיתוי (הוסר, היה 4 נק').** המאגר מכסה שנה קדימה, אז כל כנס הוא "בקרוב". הרכיב לא הבדיל בין כנסים, רק הזיז ציונים לפי תאריך שרירותי. עיתוי שייך לתצוגת התכנון, לא לציון איכות.

### תלות בקהל: מכפיל רך
בכירות, נגישות וגיאוגרפיה שוות משהו רק אם יש עם מי להיפגש. לכן הן מוכפלות ב-`0.5 + 0.5 × fit`:
- **חצי מהערך אמיתי בכל מקרה.** רצפת תערוכה היא רצפת תערוכה.
- **החצי השני תלוי בקהל.** נגישות מלאה לכנס בלי ICP שווה מעט.

**למה לא מכפיל מלא (× fit):** הוא יוצר תקרה מלאכותית. אף כנס לא יכול לעבור את fit × 100, אז Money20/20 USA, כנס ליבה, ירד ל-55 ו-MPE, המוביל, ל-68. עם המכפיל הרך התקרה היא 25 + 75 × fit.

**ההשפעה:** EuroFinance (fit 0.29) קיבל 25 נקודות נגישות מלאות ועמד על 63. עכשיו 45. WTM ו-ITB (fit 0.25): 56 ← 39.

הערך נשמר ב-`app_settings` כ-`context_fit_floor` (0.5). 0 = מכפיל מלא, 1 = בלי תלות (הנוסחה הקודמת). פירוק הציון ב-`icp_breakdown` מציג את הרכיבים אחרי ההכפלה, כך שהם מסתכמים לציון. הערכים הגולמיים נשמרים ב-`unscaled`. משפט ההסבר נשען על הערכים הגולמיים, כי "ניתן לתאם פגישות מראש" נכון בלי קשר לקהל.

### ה. סף מסה קריטית — עונש בלבד
```
נפח = משתתפים × fit
```
| נפח | עונש |
|---|---|
| מעל 400 | 0 |
| 150-400 | −8 |
| מתחת ל-150 | −15 |

**רשת ביטחון בלבד.** בפועל אף כנס במאגר לא נענש. הקטן ביותר, Phocuswright, עם כ-480 אנשי ICP, קרוב לסף של 400. אם מספר המשתתפים לא ידוע, אין עונש.

### משפט ההסבר
**במסך הכנסים הוא הוחלף בפירוט לפי רכיב ב-(i)** (ראו "החלטות עיצוב"). הוא עדיין מחושב ונשמר (`icp_explanation`), ומוצג **בתצוגת התכנון**, שם רואים מעט כנסים בכל פעם ומשפט מסכם אחד עוזר.
משפט אחד לכל ציון: עד שתי חוזקות, ואז "אבל" ועד שתי חולשות. לדוגמה: *"קהל ICP בינוני (43% PSP/תשלומים + פלטפורמות), ניתן לתאם פגישות מראש; אבל בכירות נמוכה (33%)"*.
הניסוח "חזק / בינוני / חלש" נקבע לפי **אחוז הליבה** (סגמנטים עם משקל 0.85 ומעלה, היום פלטפורמות ו-PSP: ≥50% חזק, 25–50% בינוני), **ולא לפי fit.** אחרת כנס עם הרבה קהל שאינו ליבה היה מתואר כ"קהל ICP חזק". סגמנט `other` לא נכתב כחולשה נפרדת, כי "רק X% מהקהל ב-ICP" כבר אומר את זה.

### ההמלצה (ציון + עלות)
| המלצה | תנאי |
|---|---|
| **חובה** | 60+ |
| **שווה את זה** | 55-60 ועלות לאיש ICP מתחת לחציון |
| **רק אם כבר באזור** | 40-55, או 55-60 עם עלות מעל החציון |
| **לוותר** | מתחת ל-40, או עונש מסה |

**מדד מחבר:** עלות לאיש ICP = מחיר כרטיס ÷ נפח ICP. אם העלות לא ידועה, ההמלצה לא יורדת.

### החלטות עיצוב
- **הכל ניתן לכיוון** — משקלים, חלוקת נקודות, ספים. זו התשובה ל-*"there's no single right answer"*. ⏳ הסליידרים בממשק לא נבנו. היום עורכים את `icp_formula` בטבלה. פונקציית הציון נקייה (`src/lib/icp.js`) ומוכנה לחישוב חי מסליידרים.
- **הנימוק לציון במרחק לחיצה אחת, מפורק לפי רכיב.** כפתור (i) ליד הציון פותח לכל רכיב את הנקודות שלו ושורת נימוק מהדאטה:
  `איכות קהל 36/50 · 63% מהקהל לקוח מובהק`
  `בכירות 5/10 · 35% מקבלי החלטות`
  `נגישות 26/30 · רשימת משתתפים, מערכת פגישות, רצפת תערוכה…`
  `גיאוגרפיה 9/10 · אירופה — שוק בפוקוס`
  זה יותר מידע ממשפט הסיכום היחיד שהיה על הכרטיס. **למה לא על הכרטיס:** סריקה של 23 כנסים עובדת טוב יותר עם כרטיס נקי: ציון, שם, המלצה, תאריך, עלות. הפירוט שם כשהרפ רוצה אותו. (היה: "משפט הסבר תמיד לצד הציון".) הנימוקים מחושבים ב-`componentReasons` ב-`icp.js`, מאותה דאטה שהציון משתמש בה. משפט ההסבר נשאר בתצוגת התכנון.
- **בלי עשרוניות**
- **ציונים אמורים לצאת 40-75.** בפועל: 23–75. חמישה כנסים 60+ ("חובה"), שישה מתחת ל-40 ("לוותר").
- **הרף של "חובה" הוא 60** (היה 70, ואז 65). Fintech Meetup מקבל 62 ומפעיל 50,000 פגישות 1:1 בהסכמה, הנגישות הגבוהה במאגר. הוא לא אמור לפספס "חובה" בשתי נקודות. חמישה כנסים חובה מתאימים לצוות שמגיע ל-6-8 כנסים בשנה.
- ⚠️ **כנסים על הרף:** Money20/20 USA ו-Europe על 65, Sibos 2027 על 40 בדיוק. שינוי קטן בדאטה מזיז אותם.

### בסיס
40 מתוך 100 הנקודות (נגישות + גיאו) נשענות על עובדות חד-משמעיות, לא הערכות. מאז המכפיל הרך, חצי מהן מוכפל ב-fit, שהוא הערכה.

---

## 6. רכיב 3 — תצוגת התכנון

### מטרה
לענות על **"מה דורש ממני החלטה עכשיו?"** — לא להציג לוח שנה לפענוח.

### מבנה
**מסקנות למעלה, נתונים מאחוריהן.** עד 4 כרטיסי פעולה (עם "הצג את כל ההמלצות"), ציר הזמן מאחורי כפתור.

### שלושת סוגי הכרטיסים
| סוג | מזהה | פעולה |
|---|---|---|
| **הזדמנות לחיסכון** | אשכול כנסים קרובים | "תכנן נסיעה מחוברת": פותח את הכנסים עם שיבוץ |
| **פער בכיסוי** | רבעון × אזור עם כנסים בציון 60+ ואף אחד לא משובץ | "הצג את הכנסים" |
| **התנגשות** | אותו אדם משובץ לשני כנסים בתאריכים חופפים | "העבר ל-X": העברה בלחיצה לאדם פנוי |

כל כרטיס: **מה קרה · כמה זה שווה · כפתור.**
מסודרים לפי ערך — חיסכון ראשון. להתנגשויות מובטח מקום בתוך מגבלת הכרטיסים.

### חישוב אשכולות
```
אותו region + פחות מ-14 יום בין סוף כנס לתחילת הבא = אשכול
```
לא מרחקים גיאוגרפיים. `global-rotating` לא נחשב אזור. החיסכון = הערכה גסה: **~$1,000 לכל טיסה שנחסכת.**
מוצגים רק אשכולות שיש בהם כנס משובץ או כנס בציון 55+.

**אשכול יכול לשנות המלצה:** כנס "רק אם כבר באזור" (40–55) הופך ל"שווה את זה" כשהוא צמוד לכנס משובץ.

### מדידת under-invested
לא "אין כנסים בחודש X", אלא **חוסר איזון בין מאמץ לפוטנציאל**: פערים ממוינים לפי סכום הציונים שנשאר בלי כיסוי.

### משני
- **ציר זמן:** פס אופקי אחד (משמאל לימין), נקודה מלאה = משובץ. נבנה.
- ⏳ **טבלת כיסוי** (אנשי מכירות × חודשים) לא נבנתה.

### מצב ריק
לעולם לא מסך ריק. במקום: *"השנה מכוסה היטב: N כנסים ב-M אזורים"* + ציר הזמן פתוח.

### החלטה
**המערכת מציגה, המנהל משבץ.** אין שיבוץ אוטומטי.

### סקופ
**שעה וחצי מקסימום.** לא מפת עולם, לא drag & drop, לא תחזית תקציב.

---

## 7. רכיב 4 — ממשק השטח

### מטרה
תיעוד תוך **20 שניות**, באולם רועש, עם טלפון ביד אחת.

### הנימוק העסקי
פנייה באותו יום ממירה ב-31%. אחרי 3-5 ימים — 17%.
**מהירות התיעוד היא כסף.**

### מה זה מחליף
היום הרפ כותב ב-Slack וההודעה נבלעת. כאן אותו טקסט הופך ל: שדות מובנים · חיבור להיסטוריה · דחיפה ל-HubSpot. ⏳ טיוטת מייל לא נבנתה.

### מזהי אדם
```
1. טלפון      → המזהה הראשי. ייחודי, שורד מעבר חברה
2. אימייל     → גיבוי
3. LinkedIn   → שדה העשרה, לא מזהה
4. שם + חברה  → לעולם לא ממזג לבד
```
**טלפון נשמר ב-E.164.** מספר מקומי ("050…") מקבל קידומת ברירת מחדל (972+, ניתן לשינוי במסך).
**LinkedIn:** ה-AI לא מנחש כתובות. שומר שם, מייצר קישור חיפוש, הרפ מאשר בערב.

### המסך (נבנה)
```
[ השם שלך · הכנס הנוכחי ]     ← נקבע פעם אחת לכנס

📞 טלפון *  [        ]   ← מקלדת טלפון נפתחת מיד
   ↳ "פגשת 3 פעמים, לאחרונה ב-Money20/20 Europe 2025 (יואב)"
👤 שם      [        ]
🏢 חברה    [        ]

[ 🎙️ הקלט סיכום ]
  נסה לכלול: מה הבעיה, מתי, מי מחליט
[ שמור ]

── עיבוד הקלטות ──
  סטטוס · שדות שחולצו · [מלא] בשדות ריקים
```
⏳ **לא נבנו:** צילום כרטיס ביקור, סריקת QR.

**רק הטלפון חובה.** ליד עם טלפון בלבד ניתן להשלמה תמיד; טלפון שלא נרשם אבוד לנצח.

### שלושה תרחישים
| מצב | זמן |
|---|---|
| יש כרטיס ביקור → צילום ממלא הכל | 15 שניות ⏳ |
| אין כרטיס → הקלדה | 25 שניות |
| ממש לחוץ → טלפון בלבד | 5 שניות |

### QR
**מתכננים מתוך הנחה שלא ייתן כלום.** ברוב הכנסים הוא מקודד מזהה בלבד מסיבות פרטיות, ואין API למציגים (רשימת המשתתפים היא המוצר של המארגן).
- vCard → בונוס, ממלא
- מזהה בלבד → `external_id`, להצלבה עתידית
- כשל → צילום או הקלדה

⏳ לא נבנה.

### ההקלטה הקולית — פיצ'ר ה-AI המרכזי
**רק איש המכירות, רק אחרי השיחה.** לא מקליטים את הלקוח (GDPR + תמלול באולם רועש ייכשל).

**שלושה דברים נשמרים:**
| | למי | למה |
|---|---|---|
| התמלול המלא | לאדם | הקול לא הולך לאיבוד |
| שדות מחולצים | למכונה | מיון והיסטוריה |
| **משפט זיהוי** | לאדם | *"שרה מ-PayFlow: הפזו, בוחנים ברבעון הבא, לא מחליטה"* |

**השדות המחולצים והתוויות שלהם בממשק:**
| שדה | תווית |
|---|---|
| `identity_line` | משפט זיהוי |
| `pain` | הבעיה |
| `timing` | מתי |
| `authority` | מי מחליט |
| `currencies` | מטבעות שהוזכרו (קודי ISO) |
| `next_step` | צעד הבא |

**ה-AI לא ממציא:** שדה שלא הוזכר נשאר `null` ומוצג כ-`[מלא]`, עם שדה קלט להשלמה ידנית. שדות נכתבים בלי נקודה בסוף (גם בהנחיה וגם בניקוי אחרי החילוץ).
השדות הריקים החוזרים הם מידע ניהולי: אילו שאלות הצוות לא שואל.

**מומלץ בחוזקה, לא חוסם.** מפגש בלי הקשר מסומן ⚠️ "אין הקשר" במסך האדם ולא מסומן מראש בייצוא. ⏳ תזכורת הערב לא נבנתה.

### עבודה לא מקוונת (נבנה)
**תיעוד ועיבוד נפרדים.** כל ליד, כולל ההקלטה, נשמר קודם ב-IndexedDB במכשיר ונשלח כשיש רשת. כל שלב שליחה אידמפוטנטי, כך שניסיון חוזר לא יוצר כפילויות.
תמלול וחילוץ רצים ברקע אחרי השמירה. הרפ לא מחכה, וכשל לא מאבד את הליד. אם ההקלטה עוד לא עובדה (חסר מפתח, אין רשת, כשל), היא נאספת שוב בפתיחת האפליקציה, בחזרת החיבור או בשמירת מפתח. יש גם כפתור "נסה שוב".
קליטה באולמות גרועה — בלי זה הכלי נשבר כשהוא הכי נחוץ.

### שני מצבים
**בשטח:** מהירות. בדיקת כפילות מיידית לפי טלפון: *"פגשת את שרה ב-Money20/20 2025"*. נבנה.
**בערב:** *"7 לידים. 3 חסרי הקשר, 2 חסרי טלפון."* → השלמה + טיוטות מייל. ⏳ לא נבנה (מסך הייצוא מראה חלק מזה: חסר טלפון / חסר הקשר).

---

## 8. רכיב 5 — אנשי קשר חוזרים

### מטרה
רפ פוגש ~200 אנשים בשנה ולא זוכר אף אחד.
**המערכת היא זיכרון שאפשר לשאול — לא מנגנון התראות.**

### זיהוי
| סימן | ודאות | פעולה |
|---|---|---|
| טלפון זהה | 100% | מיזוג אוטומטי — נבנה |
| אימייל / LinkedIn זהה | 95% | מיזוג אוטומטי ⏳ |
| שם דומה + אותה חברה | 70% | **"זו אותה שרה?"** ⏳ |
| שם דומה + חברה אחרת | 40% | **"אולי עברה חברה?"** ⏳ |

**מיזוג אוטומטי רק על מזהה ייחודי. כל השאר שואל**, והתשובה נשמרת ב-`merge_decisions`.
בפועל: ליד עם טלפון קיים מתווסף לאותו אדם, וחברה חדשה מעדכנת את `current_company` בלי לגעת בחברה שנשמרה במפגשים הקודמים.

**מקרי קצה:** איות (Sara/Sarah) · כינויים (Bob/Robert) · מעבר חברה · שינוי שם משפחה.

### שלוש דלתות
| כניסה | השאלה | מתי | סטטוס |
|---|---|---|---|
| 👤 לפי אדם | *"מי זה ומה היה?"* | בשטח | נבנה (חיפוש שם / טלפון) |
| 🏢 **לפי חברה** | *"מי יש לי ב-Adyen?"* | מתכננת טרגוט | נבנה. כולל אנשים שעבדו שם בזמן המפגש |
| 📅 לפי כנס | *"את מי פגשנו כאן?"* | לפני כנס חוזר | חלקי (מסך הייצוא מסונן לפי כנס) |

⏳ חיפוש גלובלי מכל מסך לא נבנה. החיפוש נמצא במסך אנשי הקשר.

### מסך אדם (מרכזי, נבנה)
```
שרה כהן · VP Treasury · FinPay
+972-50-1234567
⚠️ עבד/ה ב-PayFlow כשנפגשתם ב-2024

2026 · Money20/20 · FinPay · פגש/ה: דנה
"בוחנים ברצינות, שאלה על תמחור"
הבעיה: … | מתי: … | מי מחליט: …
🎙️ השמע הקלטה

2025 · Money20/20 · PayFlow · פגש/ה: יואב
"אותה בעיה, אמרה שיבחנו ברבעון הבא"

[ סגור ליד ]
```
⏳ הכפתורים "שלח ל-HubSpot" ו"נסח מייל" לא נבנו במסך האדם. השליחה ל-HubSpot נעשית ממסך הייצוא.

**ההיסטוריה היא הפרשנות.** אין ציון, אין מסקנה אוטומטית.
**"פגש/ה: דנה"** — אולי כדאי שדנה תיגש, לא יואב.

### שלושה תגים
| תג | מתי (כפי שמומש) | הצעה |
|---|---|---|
| ⏰ **הזמן הגיע** | במפגש האחרון `timing` = "רבעון הבא", והרבעון שאחרי המפגש כבר נגמר | *"אמר/ה X. מה קרה?"* |
| ⏸️ **תקוע** | 3+ מפגשים, ו-`timing` או `pain` זהים בשלושת האחרונים | לשאול מה חוסם |
| 💤 **רדום** | המפגש האחרון לפני 18+ חודשים | לחדש או לסגור |

כולם מבוססי **עובדות בלבד**. אפס ניחוש. ליד סגור לא מקבל תגים.
**"עבר חברה" אינו תג** — שדה מידע במסך האדם. מונע מבוכה, וחושף קשר חם בחברה חדשה.

**נמחקו:** שינוי תפקיד כללי (רעש) · הסקת סמכות (ניחוש) · דירוג אוטומטי של חברה חדשה (ניחוש) · ציון מספרי לקשר.

**הגבלות:**
- תגים ברשימה — ללא הגבלה. הם כלי מיון (צ'יפים לסינון לפי תג, עם מונה)
- התראות בבוקר כנס — קצרות, עם "הצג את כולם" ⏳

### בוקר הכנס
*"12 אנשי קשר נפגשו בכנס הזה בעבר"* — **לא "מגיעים".** אין רשימת משתתפים ולא מעמידים פנים שיש.
הכלי האמיתי בשטח: **חיפוש מהיר** → היסטוריה תוך שנייה. ⏳ הודעת הבוקר לא נבנתה.

### סגירת ליד
**תמיד ידנית.** המערכת לא מציעה לסגור.
ליד סגור לא נמחק — עובר לסטטוס `archived`, נעלם מהתגים, ההיסטוריה נשמרת. אפשר לפתוח מחדש.

### דאטת דמו (נבנה: `npm run seed:demo`)
13 אנשים, 20 מפגשים, מפוזרים בין 4 אנשי מכירות (דנה, יואב, מיכל, עומר) ו-11 מהדורות היסטוריות:
| תרחיש | מי |
|---|---|
| מתחמם + עבר חברה | Marcus Weber: PayFlow → FinPay, 2024→2026 |
| תקוע | Omar Haddad: "רבעון הבא" שלוש פעמים |
| רדום | Hannah Schmidt: אחרון ב-2024 |
| הזמן הגיע | James Carter: "רבעון הבא" באוקטובר 2025 |
| איות שונה | Sara Cohen / Sarah Cohen: אותה חברה, טלפונים שונים |
| עבר חברה, אותו טלפון | Daniel Friedman: TourNet → GlobeStay |
| רגילים | 6 אנשים, 1–2 מפגשים |

⚠️ התגים תלויים בתאריך היום. הדאטה כוילה לסוף ספטמבר 2026. אחרי 1.10.2026 עומר יקבל גם ⏰, ומ-2027 יותר אנשים יהפכו ל-💤.

---

## 9. רכיב 6 — פיצ'ר ה-AI

### המרכזי: תמלול → תיק ליד (ברכיב 4, נבנה)
```
Whisper (whisper-1, עברית) מתמלל → gpt-4o-mini מחלץ:
  תמלול מלא · שדות · משפט זיהוי
```
- החילוץ עם `response_format: json_schema` במצב strict: שישה שדות, כולם חובה וכולם nullable. `temperature: 0`.
- ההנחיה בעברית. הכלל המרכזי: *"שדה שלא הוזכר בתמלול מקבל null. אל תסיק, אל תשלים ואל תנחש."*
- פרטי הליד (שם, חברה, כנס) נשלחים יחד עם התמלול, כדי לתקן שגיאות תמלול בשמות.
- מחרוזת ריקה, רשימה ריקה או נקודה לבד הופכות ל-`null`.
- מפתח OpenAI אחד משמש לתמלול ולחילוץ. הקריאות יוצאות ישירות מהדפדפן.

**כל הקלטה מוסיפה שכבה.** אחרי שלושה כנסים יש סיפור שלם שאף אחד לא הקליד.

**למה AI הוא הכלי הנכון:** אין ולא יהיה API שהופך דיבור חופשי לשדות מובנים.
**ואם מורידים אותו — נשאר טופס.** כלומר ה-CRUD app שהבריף מזהיר מפניו.

### שימושים נוספים
| פיצ'ר | טריגר | הערה |
|---|---|---|
| אימות נתוני כנסים | Cron חודשי | תחליף ל-API שלא קיים ⏳ |
| גילוי כנסים חדשים | **כפתור ידני** | יזום. חוסך כסף, מונע רעש ⏳ |
| ניסוח מייל | כפתור במסך אדם | **טקסט להעתקה בלבד. לא שולח** ⏳ |

**החלוקה:** ה-Cron מתחזק את מה שיש. הכפתורים מרחיבים.

### העיקרון: מחלץ ומנסח — לא מסיק ולא מכריע
| נפסל | למה |
|---|---|
| ניחוש כתובות LinkedIn | קישור שגוי גרוע מקישור חסר |
| הסקה שמישהו קודם | רואים טקסט שהשתנה, לא סמכות |
| דירוג אוטומטי של חברה | ניחוש. הרפ ישאל בשיחה |
| הכרעה מתחמם/tire-kicker | ההיסטוריה מדברת בעצמה |

---

## 10. רכיב 7 — HubSpot

### המודל
| | מי | למה |
|---|---|---|
| **מה נשלח** | הרפ מסמן | באולם מתעדים גם ספקים ומתחרים |
| **השליחה** | **אוטומטית לגמרי** | אף אחד לא נוגע בקובץ |

**סימון מראש (נבנה):** מסך "ייצוא" מציג את הלידים של כנס נבחר, ליד אחד לכל אדם. מסומנים מראש רק מי שיש לו **טלפון והקשר** (תמלול או משפט זיהוי). ליד שלא סומן מראש מציג את הסיבה. הרפ רק מוריד סימון. **נגיעה אחת במקום עשרים.**

```
הרפ מסמן → המערכת מייצרת CSV → מעלה דרך Edge Function → ✓ הייבוא הושלם
```

### טכנית
- **`POST /crm/v3/imports`** עם מיפוי עמודות. CSV
- **למה Import API ולא יצירה אחד-אחד:** קריאה אחת במקום 25, HubSpot מטפל בכפילויות
- **דרך Supabase Edge Function `hubspot-import`:** דפדפן לא יכול לקרוא ישירות (CORS). בשונה מהתוכנית, ה-token לא יושב כ-Secret בפונקציה: הוא מוזן במסך ההגדרות ונשלח בכל קריאה. הפונקציה לא שומרת סוד ויכולה רק לבדוק token, להתחיל ייבוא ולבדוק סטטוס.
- מיפוי העמודות קבוע בתוך הפונקציה (לא מגיע מהלקוח), כך שאי אפשר לייבא לשום אובייקט מלבד אנשי קשר וחברות.
- אחרי השליחה, המסך בודק את סטטוס הייבוא כ-30 שניות ומציג את התוצאה.
- **הרשאות ה-token:** `crm.import`, `crm.objects.contacts.write`, `crm.objects.companies.write`, `crm.schemas.contacts.write`
- **סטטוס:** הפונקציה כתובה (`supabase/functions/hubspot-import/index.ts`) ⏳ **אבל לא פרוסה ולא נבדקה מול חשבון HubSpot אמיתי.** הפריסה דרך ה-Dashboard של Supabase, עם "Verify JWT" כבוי (הפרויקט משתמש במפתח publishable שאינו JWT).

### מה נשלח
| ב-HubSpot | מה | עמודה ב-CSV |
|---|---|---|
| Contact | שם פרטי, שם משפחה, טלפון, מייל, תפקיד | First Name, Last Name, Phone Number, Email, Job Title |
| Company | שם החברה, משויכת לאיש הקשר באותה שורה | Company |
| **תיק הליד** | משפט הזיהוי + כל המפגשים בכל הכנסים: שנה, כנס, חברה, מי פגש, מה נאמר, השדות והתמלול — **כאן הערך** | Grain Lead History → שדה מותאם `grain_lead_history` (נוצר אוטומטית בייבוא הראשון) |
| ~~Deal~~ | **לא.** ליד מכנס אינו עסקה | — |

⚠️ **סיכון פתוח:** לפי התיעוד, ייבוא של כמה אובייקטים בקובץ אחד דורש מזהה ייחודי לכל אובייקט (מייל לאנשי קשר, דומיין לחברות). לרוב הלידים שלנו יש רק טלפון. אם HubSpot ידחה שורות, מורידים את מיפוי החברה ומייבאים אנשי קשר בלבד.

### גיבוי (נבנה ונבדק)
כפתור "הורד CSV" תמיד זמין: אין עדיין token · רוצים לבדוק לפני · HubSpot לא זמין. בלי token מוצג רק כפתור ההורדה, עם הסבר.
הקובץ נפתח נכון באקסל (BOM לעברית), ותאים שמתחילים ב-`=` או `@` מנוטרלים כדי שאקסל לא יריץ אותם כנוסחה.

---

## 11. מה לא בסקופ

| | למה | מתי |
|---|---|---|
| שליחת מייל אוטומטית | החלטה עסקית, לא טכנית. GDPR | שלב הבא |
| סנכרון דו-כיווני מ-HubSpot | פי 3 עבודה. הבריף ביקש *path to push* | שלב הבא |
| מפת עולם אינטראקטיבית | נראית טוב, לא מוסיפה מידע | — |
| Drag & drop לשיבוץ | שעתיים על קסם | — |
| שיבוץ אנשים אוטומטי | דורש דאטה שאין | — |
| התחברות ל-10times/Eventbrite | נבדק ונפסל | — |
| התחברות משתמשים והרשאות | כלי פנימי. המפתח הציבורי כותב לכל הטבלאות | שלב הבא |

---

## 12. שאלות פתוחות ל-Grain

**נענו בראיון:**
- **בנקים:** מתחרה, לא לקוח (משקל 0.1).
- **מי ה-ICP:** פלטפורמות ומרקטפלייסים שמזיזים כסף בשם אחרים, לא עסקים בודדים עם חשיפת מט"ח. טרז'רי וסיטונאי תיירות ירדו ל-0.3 (סעיף 5).

**נפתחו בעקבות הגדרת ה-ICP:**
- **החלוקה לפי כנס היא הערכה שלי.** מאילו כנסים הגיעו בעבר לידים של פלטפורמות? זה הנתון הכי טוב לכייל מולו את `audience_mix`, במיוחד ב-Money20/20, MPE ו-Phocuswright.
- **המשקלים של הסגמנטים החדשים:** האם פינטק משובץ (0.7) ו-SaaS ורטיקלי (0.5) הם ICP? כלומר, האם Grain מוכרת גם לחברה שמטמיעה תשלומים בתוכנה ענפית, או רק למי שכבר מזיז כסף בהיקף?

**שאלות שנפתחו בבנייה:** מהם שווקי הפוקוס של Grain (הונח: אירופה וצפון אמריקה)? (שאלת חלון העיתוי ירדה: רכיב העיתוי הוסר מהציון, סעיף 5.)

**גיבוי אם אין תשובה:** שני הערכים נמצאים ב-`app_settings` וניתנים לשינוי בלי קוד.

---

## 13. נתונים שנאספו במחקר

| ממצא | מקור | שימוש |
|---|---|---|
| 5-10 פגישות מוכשרות במודל תנועה בביתן · 25-50 עם קביעה מראש | Vendelux | סף מסה קריטית, משקל הנגישות |
| 76% מלוחות הזמנים נקבעים לפני ההגעה | Event Marketing Research | Conference Brief מראש |
| 70% מציינים חוסר נראות לרשימת משתתפים כמחסום | Vendelux 2026 | הצדקת הרכיב |
| המרה: 31% באותו יום, 17% אחרי 3-5 ימים | ExpoTraffic | מהירות התיעוד |
| Money20/20 USA: 18-21.10.2026, Venetian, ~11-13K, 33% C-Suite, מ-$3,599 | אתר רשמי + משניים | דוגמת חישוב |

**סייג:** חלק מהמקורות הם בלוגים של ספקי כלים לכנסים, עם אינטרס. הטווחים עקביים בין מקורות — להציג כ"מה שהתעשייה מדווחת".

---

## 14. סדר בנייה מומלץ

| # | משימה | זמן | סטטוס |
|---|---|---|---|
| 1 | Supabase + סכמה + seed | 45 דק' | ✅ |
| 2 | רשימת כנסים + ציון + פילטרים | 60 דק' | ✅ (בלי סליידרים) |
| 3 | **ממשק שטח** (רכיב 4) | 90 דק' | ✅ טופס, הקלטה, עבודה לא מקוונת, AI · ⏳ צילום, QR, מצב ערב |
| 4 | **מסך אדם + זיהוי** (רכיב 5) | 90 דק' | ✅ חיפוש, היסטוריה, תגים · ⏳ שאלת מיזוג |
| 5 | תצוגת תכנון (רכיב 3) | 60 דק' | ✅ (בלי טבלת כיסוי) |
| 6 | HubSpot | 30 דק' | ✅ CSV · ⏳ פריסת Edge Function |
| 7 | Cron + גילוי כנסים | 30 דק' | ⏳ |
| 8 | ליטוש ודמו | 45 דק' | ✅ דאטת דמו |

**רכיבים 4 ו-5 הם לב ההערכה.** אם נגמר הזמן — הם חייבים להיות מושלמים, השאר יכולים להיות בסיסיים.

---

## 15. יומן שינויים

| תאריך | שינוי | סיבה |
|---|---|---|
| 2026-09-24 | **מסך הכנסים: נימוק לפי רכיב, כרטיס נקי, פילטרים מקובצים.** **(1)** ב-(i) ליד הציון: לכל רכיב הנקודות ושורת נימוק מהדאטה (`componentReasons` ב-`icp.js`), למשל *"איכות קהל 36/50 · 63% מהקהל לקוח מובהק"*, ושורת עונש מסה כשיש. **(2)** משפט ההסבר (`icp_explanation`) הוסר מכרטיס הכנס. תג ההמלצה נשאר. המשפט עדיין מוצג בתצוגת התכנון. העיקרון בסעיף 5 עודכן ("משפט הסבר תמיד לצד הציון" ← "הנימוק במרחק לחיצה, לפי רכיב"). **(3)** הצ'יפים הוחלפו בארבעה תפריטים נפתחים בשורה אחת: המלצה, אזור, תאריך, עלות, ו"נקה הכל". בחירה פעילה מוצגת על הכפתור, ספירות בתוך כל פאנל, סגירה בלחיצה בחוץ או Escape, פאנל ברוחב מלא במובייל. **פילטר תאריך חדש** (רבעון או חודש ספציפי, רק כאלה שיש בהם כנסים) במקום "הרבעון הבא". **(4)** בתיבת "תמלול AI" במסך השטח נוסף "להגדרת מפתח ←", וה-⚠️ הוסר מלשונית ההגדרות בסרגל העליון | סריקה של 23 כנסים עובדת טוב יותר עם כרטיס נקי: ציון, שם, המלצה, תאריך, עלות. הפירוט לפי רכיב נותן יותר מידע ממשפט הסיכום, והוא שם כשהרפ רוצה אותו. בתצוגת התכנון רואים מעט כנסים בכל פעם, ושם משפט מסכם עוזר. ארבעה כפתורים תופסים שורה אחת במקום שלוש שורות צ'יפים. ⚠️ על לשונית נראה כמו תקלה, כשבפועל חסר מפתח הוא שלב הגדרה |
| 2026-09-24 | **נגישות וקריאות.** `--muted` הוכהה ל-`#4A5764` (4.6:1 ← 7.4:1 על לבן; על רקע העמוד היה 4.25:1, מתחת לסף). תוכן ראשי עבר ל-`--text` (תאריך/עיר/עלות, ערכי שדות, תמלולים ומשפטי זיהוי, תפקיד וחברה, טלפון), ו-muted נשאר לתוויות, כותרות וספירות. מינימום 14px לתוכן (13px רק לתוויות וספירות), תגים 12 ← 13px. גובה שורה 1.5 ← 1.6. `opacity` הוסר מספירות הצ'יפים. כל התגים וההודעות נמדדו: 4.87:1 ומעלה | יותר מדי מידע חשוב היה באפור בהיר וקטן. עברית צריכה יותר מקום בין שורות |
| 2026-09-24 | **אזור לפי המהדורה, לא לפי הסדרה.** סדרה מתחלפת (Sibos, `global-rotating`) משויכת בכל שנה לאזור של המדינה שבה היא נערכת (`editionRegion` ב-`format.js`). **(1) ציון:** רכיב הגיאוגרפיה ב-`icp.js` קורא את אזור המהדורה. Sibos 2026 (מיאמי, צפון אמריקה) 40 ← 46 (מקום 17 ← 12), עדיין "רק אם כבר באזור". Sibos 2027 (סינגפור) לא זז: 40. רק ציון אחד השתנה. **(2) תכנון:** אשכולות (`findClusters`), פערי כיסוי (`findGaps`) וספירת האזורים ב-`Planning.jsx` כבר לא מדלגים על מהדורות מתחלפות. בדאטה הנוכחית אף אשכול לא השתנה: Sibos 2027 רחוק 10 חודשים מ-SFF 2026 (אין SFF 2027 במאגר), ו-Sibos 2026 נגמר 17 יום לפני Money20/20 USA (הסף: פחות מ-14). נבדק עם מהדורת בדיקה בזיכרון ש-Sibos אכן מתקבץ עם כנס סמוך באותו אזור. גם פילטר האזור במסך הכנסים משתמש באותה פונקציה | הציון נתן ל-Sibos 2026 אפס נקודות גיאוגרפיה כי "מתחלף" אינו שוק בפוקוס, בזמן שמיאמי כן. ותצוגת התכנון התעלמה ממנו לגמרי, כך שנסיעה משותפת עם כנס סמוך לא הייתה מזוהה. עכשיו הציון, הפילטר והתכנון מסכימים על אזור אחד לכל מהדורה |
| 2026-09-24 | **בכירות, נגישות וגיאוגרפיה תלויות עכשיו בקהל: מוכפלות ב-`0.5 + 0.5 × fit`** (`context_fit_floor` ב-`app_settings`). **הרף של "חובה" ירד מ-65 ל-60** (`planning.js`, וגם צבע האריח ב-`Conferences.jsx`: 60+ כהה, 55+ בינוני). מכפיל מלא (× fit) נבדק בהרצה יבשה ונפסל. **השפעה:** EuroFinance 63 ← 45 (מקום 9 ← 13), WTM ו-ITB 56 ← 39 (עברו ל"לוותר"), Phocuswright 65 ← 54, MPE 81 ← 75, Money20/20 Miami 76 ← 67, USA ו-Europe 74 ← 65, Fintech Meetup 72 ← 62, Seamless North Africa 50 ← 46 (מקום 18 ← 12, קהל טוב ונגישות חלשה). **"חובה" (60+):** MPE, Money20/20 Miami, USA ו-Europe, Fintech Meetup. **"לוותר" (מתחת ל-40):** WTM, ITB, AFP, SaaStr, Web Summit, Collision. טווח: 23–75 | נגישות, בכירות וגיאוגרפיה שוות משהו רק אם יש עם מי להיפגש. בחלוקה הקודמת EuroFinance, עם 5% ICP, קיבל 63 בזכות נגישות מלאה. **למה רך:** מכפיל מלא יוצר תקרה של fit × 100, ואז Money20/20 USA, כנס ליבה, יורד ל-55. חצי מהערך אמיתי בכל מקרה (רצפת תערוכה היא רצפת תערוכה), חצי תלוי בקהל. **למה 60:** Fintech Meetup (62) מפעיל 50,000 פגישות 1:1, הנגישות הגבוהה במאגר, ולא אמור לפספס בשתי נקודות. חמישה כנסים חובה מתאימים לצוות של 6-8 כנסים בשנה |
| 2026-09-24 | **כיול חלוקת הנקודות: קהל 50 (היה 45), בכירות 10 (היה 20), נגישות 30 (היה 25), גיאוגרפיה 10 בלי עיתוי (היה 6+4).** רכיב העיתוי הוסר מהקוד ומ-`app_settings` (`geo_timing` ← `geo`). פריטי הנגישות: 10/8/6/4/2. גם הסף של "ניתן לתאם פגישות מראש" במשפט ההסבר הפך יחסי (80% מרכיב הנגישות במקום 20 קבוע). **השפעה:** Money20/20 Europe 67 ← 74 (הרוויח את 4 נק' העיתוי שחסרו לו), Asia 61 ← 65, Phocuswright 61 ← 65, Seamless ME 55 ← 59, Collision 32 ← 37. ירדו: AFP 47 ← 43, Paris Fintech Forum 62 ← 59, Sibos 2026 55 ← 52. **EuroFinance כמעט לא זז: 64 ← 63** (מקום 6 ← 9). הבכירות ירדה ב-8 אבל הנגישות עלתה ב-5. ב"חובה" (65+) עכשיו שבעה במקום חמישה: MPE 81, Money20/20 Miami 76, USA 74, Europe 74, Fintech Meetup 72, Money20/20 Asia 65, Phocuswright 65. טווח: 37–81 | **עיתוי:** המאגר מכסה שנה קדימה, אז כל כנס "בקרוב". הרכיב לא הבדיל, רק הזיז ציונים לפי תאריך שרירותי. עיתוי שייך לתצוגת התכנון. **בכירות:** איש מכירות טוב נכנס דרך מי שהוא פוגש ומטפס למקבל ההחלטות. במשקל 20 היא החזיקה את EuroFinance (קהל 13/45, סה"כ 64) |
| 2026-09-24 | **הגדרת ה-ICP הוחלפה: פלטפורמות, לא עסקים בודדים.** סגמנטים חדשים: `platforms` (1.0), `embedded_fintech` (0.7), `saas_vertical` (0.5), ו-`other` מפורש (0). `treasury` 0.85 ← 0.3, `travel_wholesale` 0.9 ← 0.3, `travel_general` 0.15 ← 0.1. `fintech_general` הוסר. כל 22 הסדרות חולקו מחדש ב-`seed/series.json` ונטענו מחדש (סך 100 בכל אחת), והמשקלים הוחלפו ב-`app_settings`. **השפעה:** EuroFinance 81 ← 64 (מקום 1 ← 6, יצא מ"חובה"). AFP 62 ← 47 (מקום 8 ← 19). MPE 76 ← 78 ועלה למקום 1. Money20/20 Miami 69 ← 72, USA 69 ← 71, Europe 65 ← 67, Asia 58 ← 61. Fintech Meetup 66 ← 70. WTM 57 ← 53, ITB 55 ← 52. Sibos 2026 59 ← 55. Phocuswright כמעט לא זז (62 ← 61): הפלטפורמות שנוספו קוזזו בירידת הסיטונאים. ב"חובה" חמישה: MPE, Money20/20 Miami, USA ו-Europe, Fintech Meetup. גם `mergeSettings` מחליף עכשיו את `segment_weights` בשלמותו ולא ממזג, כדי שסגמנט שהוסר לא יחזור מברירות המחדל | ראיון עם Grain: הם מוכרים לפלטפורמות ולמרקטפלייסים שמזיזים כסף בשם אחרים (Booking, לא המלון). ההגדרה הקודמת ("כל עסק עם חשיפת מט"ח") דירגה כנסי טרז'רי במקום הראשון, והם מלאים בחברות שמגדרות לעצמן |
| 2026-09-24 | **הרף של "חובה" ירד מ-70 ל-65** (`recommendation` ב-`src/lib/planning.js`, וגם צבע אריח הציון ב-`Conferences.jsx`). הטווחים של "שווה את זה" ו"רק אם כבר באזור" הם עכשיו 55-65. שישה כנסים ב"חובה": EuroFinance (81), MPE Berlin (76), Money20/20 USA ו-Miami (69), Fintech Meetup (66), Money20/20 Europe (65) | שינוי משקל הבנקים הוריד את כל הסקאלה, כי כמעט לכל כנס פינטק יש 25%-27% בנקים בקהל. סדר הדירוג נכון, אבל הרף כויל לסקאלה הישנה. שישה כנסים ב"חובה" הגיוני לצוות שמגיע ל-6-8 כנסים בשנה. Money20/20 הוא בבירור חובה ל-Grain, ואסור שייצא רק כי הסקאלה זזה |
| 2026-09-24 | **משקל הבנקים ירד מ-0.4 ל-0.1** (ב-`app_settings` וב-`icpDefaults.js`). השאלה הפתוחה בסעיף 12 נסגרה. **Sibos 2026:** 67 ← 59 (מקום 7 ← 11). **Sibos 2027:** 63 ← 55 (מקום 11 ← 15), בדיוק על הרף של "שווה את זה". **EBAday 2027:** 60 ← 54 (מקום 15 ← 17), ירד מתחת ל-55 ל"רק אם כבר באזור". תופעת לוואי: Money20/20 USA ו-Miami (72 ← 69) ו-Fintech Meetup (70 ← 66) ירדו מתחת ל-70 ויצאו מ"חובה". רק EuroFinance ו-MPE נשארו שם | ראיון עם Grain: בנקים הם המתחרה ולא הלקוח. עסק שרוצה לגדר מט"ח הולך היום לבנק שלו, ו-Grain מחליפה את זה. לא 0, כי יש ערך של מודיעין תחרותי ושותפויות |
| 2026-09-24 | **חילוץ עם OpenAI `gpt-4o-mini` במקום Claude.** Structured output דרך `json_schema` במצב strict, אותה הנחיה בעברית. מפתח OpenAI אחד לתמלול ולחילוץ, והמפתח של Anthropic הוסר מההגדרות ומהקוד | אין חשבון Anthropic |
| 2026-09-23 | **סגמנט התיירות פוצל:** `travel_wholesale` (0.9) ו-`travel_general` (0.15). ITB, WTM ו-Phocuswright קיבלו חלוקה ידנית, ותיירות קטנה בשאר הכנסים עברה ל-`travel_general` | משקל יחיד של 0.9 לכל התיירות גרם לכנסי תיירות (WTM 79, ITB 77, Phocuswright 76) לעקוף את Money20/20. ה-0.9 נועד לסיטונאים בלבד, לא למלונות וליעדים. אחרי הפיצול: WTM 57, ITB 55, Phocuswright 62 |
| 2026-09-24 | **תוויות השדות בעברית פשוטה:** כאב → **הבעיה**, עיתוי → **מתי**, סמכות → **מי מחליט**, מטבעות → **מטבעות שהוזכרו**. גם בהנחיית החילוץ. נוסף: שדות בלי נקודה בסוף, ורמז מתחת לכפתור ההקלטה | שפה של איש מכירות ולא של אנליסט |
| 2026-09-24 | **HubSpot:** מסך ייצוא עם סימון מראש, והורדת CSV עובדת ונבדקה. Edge Function נכתבה אבל לא נפרסה ולא נבדקה מול HubSpot. ה-token נשמר בהגדרות ולא כ-Secret | הפריסה דורשת גישה ל-Dashboard של Supabase ו-token של HubSpot |
| 2026-09-23 | **`ticket_cost` → `ticket_cost_usd`** | שם השדה ב-seed ובטבלה. המטבע מפורש בשם |
| 2026-09-23 | `rep_id` → `rep_name` (טקסט) בטבלת `encounters` | אין טבלת נציגים. שם מספיק לדמו ולתצוגת התכנון |
| 2026-09-23 | טבלאות חדשות: `app_settings` (הגדרות הנוסחה) ו-`edition_assignments` (שיבוץ). עמודות חדשות: `icp_explanation`, `icp_breakdown`, `audio_path`, `status` | משקלים לא בקוד · תצוגת התכנון צריכה שיבוצים · הסבר ופירוק לצד הציון · קישור להקלטה |
| 2026-09-23 | ניסוח "קהל ICP חזק / בינוני" לפי אחוז סגמנטי הליבה ולא לפי fit | Sibos (55% בנקים, 20% PSP) תואר בטעות כ"קהל ICP חזק" |
| 2026-09-23 | ציון הגיאו/תזמון: אירופה + צפון אמריקה, חלון של 6 חודשים | ה-PRD לא הגדיר. הנחה עד אישור מ-Grain |
| 2026-09-23 | HashRouter (כתובות `#/…`) | פריסה בגרירה בלי הגדרות הפניה בשרת |

---

## 16. מערכת העיצוב (כפי שמומשה)

כל הטוקנים מוגדרים כמשתני CSS ב-`src/styles.css`.

### עקרונות
- **נקי ומרווח.** בלי צללים, בלי גרדיאנטים. הפרדה באמצעות מסגרת דקה ורקע.
- **RTL מלא** (`<html dir="rtl" lang="he">`). מספרי טלפון וקידומות מוצגים LTR. ציר הזמן בתכנון רץ משמאל לימין.
- **מובייל קודם למסך השטח** (עמודה צרה של 520px, כפתורים ברוחב מלא).

### צבעים
| טוקן | ערך | שימוש |
|---|---|---|
| `--primary` | `#0F2B45` | סרגל עליון, כותרות, טקסט על אריח ציון גבוה |
| `--action` | `#1E5F8C` | כפתורים, קישורים, focus, נקודות בציר הזמן |
| `--text` | `#1A1A1A` | טקסט ראשי |
| `--muted` | `#4A5764` | **רק** תוויות שדות, כותרות משנה, ספירות ומטא-דאטה משנית באמת. 7.4:1 על לבן (היה `#6B7785`: 4.6:1, ורק 4.25:1 על רקע העמוד) |
| `--border` | `#E4E8EC` | מסגרות כרטיסים ושדות |
| `--card` | `#FFFFFF` | כרטיסים ושדות |
| `--bg` | `#F5F7F9` | רקע העמוד |
| `--accent` | `#1BA8B8` | **הדגשה (טורקיז), רק למה שהכי חשוב:** אריח ציון 60+, פס מתחת לקישור הפעיל בסרגל העליון, מסגרת צ'יפ פעיל |
| `--accent-strong` | `#0B6E79` | טקסט טורקיז על רקע בהיר: תג "חובה", צ'יפ פעיל, כפתור (i) |
| `--accent-soft` / `--accent-border` | `#E8F7F9` / `#A9DEE4` | רקע ומסגרת לתג "חובה", לצ'יפ פעיל ולכפתור (i) |

**טורקיז כהדגשה בלבד, לא כצבע ראשי שני.** כלי B2B בפינטק: הבסיס נשאר כחול-אפור. ⚠️ לבן על `#1BA8B8` הוא 2.9:1 ולא עומד ב-WCAG, לכן על רקע טורקיז הטקסט הוא `--primary` (4.9:1), וטקסט טורקיז על רקע בהיר הוא `--accent-strong` (5.5:1).

**צבעים סמנטיים** (לתגים ולהודעות, כל אחד עם רקע בהיר ומסגרת):
| טון | טקסט | רקע | מסגרת | שימוש |
|---|---|---|---|---|
| accent | `#0B6E79` | `#E8F7F9` | `#A9DEE4` | "חובה" |
| good | `#1F7A4D` | `#F1F9F5` | `#BFE0CF` | חיסכון, ⏰, נשמר |
| info | `#1E5F8C` | `#F1F6FA` | `#C8DBEA` | "שווה את זה", 💤, משובצים |
| warn | `#A15C00` | `#FDF7EE` | `#F0D9B5` | "רק אם כבר באזור", ⏸️, חסר, `[מלא]` |
| bad | `#A23B3B` | `#FCF3F3` | `#ECC8C8` | "לוותר", התנגשות, שגיאות |

### טיפוגרפיה
- **גופן:** `system-ui, -apple-system, 'Segoe UI', Arial, sans-serif`. אין טעינת גופנים חיצוניים.
- **בסיס:** 15px, גובה שורה 1.6 (עברית צריכה יותר מקום מלטינית)
- **H1:** 22px, בצבע primary · **H2:** 17px, בצבע primary · **שם כנס בכרטיס:** 18px מודגש
- **מינימום 14px לכל תוכן שהרפ קורא.** 13px (`.small`) רק לתוויות, כותרות משנה וספירות.
- **תוכן ראשי בצבע `--text`, לא muted:** תאריך, עיר ועלות בכרטיס כנס · ערכי שדות במסך אדם (התוויות נשארות muted) · תמלולים ומשפטי זיהוי · תפקיד וחברה מתחת לשם · טלפון
- **תגים:** 13px · **ציון באריח:** 28px, 800
- **ניגודיות:** כל שילוב טקסט-רקע לפחות 4.5:1 (נמדד). הנמוך ביותר: תג warn, 4.87:1. בלי `opacity` על טקסט (הוריד את ספירת הצ'יפ הפעיל ל-3.2:1). כפתור מושבת עם `opacity: 0.5` מוחרג ב-WCAG
- כפתור גדול (מסך שטח): 17px

### מרווחים ופריסה
| | ערך |
|---|---|
| רוחב עמוד | 960px מקסימום · 520px למסכים צרים (שטח, הגדרות) |
| ריפוד עמוד | 24px למעלה, 16px בצדדים, 64px למטה (16px למעלה במובייל) |
| רווח בין כרטיסים | 12px |
| רווח בשורה (chips, meta) | 8px |
| ריפוד כרטיס | 16px · כרטיס צפוף: 12px × 14px |
| נקודת שבירה | 520px |
| Safe area | הסרגל העליון והפס הדביק בייצוא מוסיפים `env(safe-area-inset-*)` |

### כרטיס
- רקע לבן, מסגרת `1px solid #E4E8EC`, **רדיוס 12px**, ללא צל.
- **כרטיס כנס:** אריח ציון 56×56 (רדיוס 12px) משמאל לתוכן, בגריד `56px 1fr`.
  - ציון 60+ (`hi`): רקע accent (טורקיז), טקסט primary
  - 55–59 (`mid`): רקע `#EAF1F7`, טקסט primary
  - מתחת ל-55 (`lo`): טקסט muted
- **כרטיס מפגש (מסך אדם):** קו צד של 3px בצבע border בצד ההתחלה.

### רכיבים
| רכיב | סגנון |
|---|---|
| כפתור ראשי | רקע action, טקסט לבן, רדיוס 10px, ריפוד 10×16px |
| כפתור משני | רקע לבן, טקסט ומסגרת action |
| כפתור שקוף (ghost) | ללא רקע ומסגרת, טקסט action |
| כפתור גדול | רוחב מלא, ריפוד 14px, 17px |
| שדה קלט | ריפוד 12px, רדיוס 10px, מסגרת border. Focus: קו מתאר 2px בצבע action |
| צ'יפ סינון | גלולה (רדיוס 999px), מסגרת border, 14px. פעיל: רקע primary |
| תג | גלולה, 12px, בטון סמנטי |
| הודעה (notice) | רדיוס 12px, מסגרת ורקע בטון סמנטי |
| סרגל עליון | רקע primary, קישורים `#C9D6E2`, פעיל: לבן על רקע לבן שקוף 12% |
| ציר זמן | קו עליון בצבע border, קווי חודש מקווקווים, נקודה 12px עם מסגרת action (מלאה = משובץ) |
| הקלטה פעילה | נקודה אדומה 10px (bad) בתוך הכפתור |
